<ul>
  <% @db_schemas.each do |db_schema| %>
    <li><%= link_to db_schema.name, db_schema_path(db_schema) %> <%= link_to db_schema_sharings_url(db_schema, token: db_schema.token), db_schema_sharings_path(db_schema, token: db_schema.token)%> </li>
  <% end %>
</ul>
