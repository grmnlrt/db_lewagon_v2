<ul>
  <% @my_db_schemas.each do |db_schema| %>
    <li><%= link_to db_schema.name, db_schema_path(db_schema) %> <%= db_schema_sharings_url(db_schema, token: db_schema.token) %> </li>
  <% end %>
</ul>
<ul>
  <% @db_schema_sharing.each do |db_schema| %>
    <li><%= link_to db_schema.name, db_schema_path(db_schema) %> <%= db_schema_sharings_url(db_schema, token: db_schema.token) %> </li>
  <% end %>
</ul>
<div class="wrapper">
  <div class="container">
    <div class="schemas my-schemas">
      <div class="schemas-headbar my-schemas-headbar">
      </div>
      <div class="schemas-cards my-schemas-cards">
        <div class="card">
        </div>
      </div>
    </div>
    <div class="schemas other-schemas">
      <div class="schemas-headbar other-schemas-headbar">
      </div>
      <div class="schemas-cards other-schemas-cards">
        <div class="card">
        </div>
      </div>
    </div>
  </div>
</div>
